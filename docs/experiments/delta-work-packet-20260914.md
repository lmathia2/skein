# Delta work-packet and review-control live experiment ledger

Date: 2026-09-14/15 UTC. This is retained experimental evidence; the active context
contract is documented in [Context and memory](../adr/context-and-memory.md).

## Diagnostic continuation canary: complete

Raw results: `/Users/mathiasl/skein-eval-results/delta-work-packet-canary-20260914` (`results.json`, six per-trial `result.json` files, wire bodies, canonical events). OpenRouter `openai/gpt-5.6-luna`/max; three consumed development cases, one attempt per paired arm, concurrency six, common command image `sha256:c50fcbacd80c6e4b42e18fedf0f8f4bcb2c591ef9a04e4dba39e588e095d4b8e`.

| Case | Baseline / delta verified | Model calls | Uncached input | Provider cost | Wall seconds | Completed post-cut source reads |
| --- | --- | ---: | ---: | ---: | ---: | --- |
| routing | yes / yes | 6 / 4 | 17,392 / 14,985 | $0.009639 / $0.008783 | 43.8 / 40.2 | 0 / 0 |
| restart | yes / yes | 4 / 4 | 21,118 / 16,590 | $0.011451 / $0.008869 | 48.6 / 43.7 | 0 / 0 |
| freshness | yes / yes | 7 / 5 | 19,528 / 15,942 | $0.014490 / $0.008946 | 69.1 / 39.0 | 1 / 1, required changed-version source |

All six answers had completed required source ranges before write, and no unaccounted or extra wire attempts. The paired totals are 17 / 13 model calls, 58,038 / 47,517 uncached input tokens, and $0.035580 / $0.026598. **This is not causal evidence for delta packets:** each continuation exposed only one full `work_batch_navigation` snapshot, no navigation patch, and these trials did not invoke the outer workflow's work-packet/review boundary. The task-level differences can be stochastic. The canary establishes no obvious completion regression in these three cases and no avoidable reread advantage; baseline already did not reread unchanged sources.

## DeepSWE full-workflow diagnostic: negative

Frozen paired task: DeepSWE 1.1 `ipython-session-bundle-replay`, OpenRouter Luna/max, 32,768 output reserve, 1B cumulative input ceiling, one attempt, zero retries, identical confirmation manifest and Harbor verifier. Baseline profile `notebook-ptc-memory.yaml`; treatment profile `notebook-ptc-memory-delta.yaml` differs only by `context.delta_work_packets: true`.

- Baseline: `/Users/mathiasl/skein-eval-results/delta-deepswe-ipython-baseline-20260914`
- Delta: `/Users/mathiasl/skein-eval-results/delta-deepswe-ipython-delta-20260914`

The task has earlier paid results, so this pair is a diagnostic mechanism/regression check, not held-out quality evidence. Optional-event type guards were changed in source after processes launched; they are behavior-equivalent, but this first pair is conservatively tagged as a mixed-source diagnostic.

| Arm | Harbor reward | Local terminal | Changed paths | Model calls | Uncached input | Cost | Active task wall |
| --- | ---: | --- | ---: | ---: | ---: | ---: | ---: |
| Memory baseline | 1 | blocked | 5 | 49 | 218,124 | $0.213668 | 676s |
| Delta + review guard | 0 | blocked | 0 | 14 | 44,465 | $0.022247 | 135s |

The lower calls/tokens/cost are **early termination**, not improved efficiency. The treatment published one full 13,666-byte work packet and two patches of 26,969 and 17,884 bytes. Its navigation exposure was one full 11,731-byte snapshot and one 7,770-byte patch. The first work-packet patch was larger than the initial full packet because 11,751 bytes of new navigation and 8,966 bytes of recent events arrived. Its next patch still included 8,803 bytes of recent events; these were largely duplicate notebook/cell payload records with verbose state manifests. The baseline yielded `review_cell_limit` repeatedly. In treatment, sequence 137 yielded review once, sequence 144 rejected another tool, and sequence 147 terminally blocked the task without any source mutation. The review scaffold said to make a correction if needed while the tool guard prohibited one and immediately forced a blocked response after the first rejected attempt. This is a protocol/usability failure, not a verifier failure.

Correction group after this negative live result: hold the tool guard during one structured decision batch, but after the first denied tool give the model a chance to return `verify` or `continue` with concrete defect/next correction; only repeat tool attempts terminally block. Explicitly say that correction tools become available in the *following implement batch*. The treatment also projects recent events as small event-ID-addressed receipts and excludes duplicate notebook transport records; canonical events and navigation remain unchanged. Deterministic tests establish the guard/projection contracts. A new paid pair tests their value; this original pair is not selectively retried as a promotion result.

## E3 corrected full-workflow paired diagnostic: complete, mixed

Two tasks (`actionlint-action-pinning-lint`, `ipython-session-bundle-replay`) were frozen before dispatch. Four trials ran concurrently across two arms using the E2 Luna/max, manifest, output/input ceilings, one-attempt, zero-retry and Harbor contracts. These are used diagnostic tasks, not held-out promotion evidence. No source edits are permitted until all four finish.

- Baseline: `/Users/mathiasl/skein-eval-results/delta-deepswe-e3-baseline-20260914`
- Corrected delta: `/Users/mathiasl/skein-eval-results/delta-deepswe-e3-delta-20260914`

| Task | Harbor reward baseline / delta | Model calls | Uncached input | Cost | Active task wall |
| --- | --- | ---: | ---: | ---: | ---: |
| actionlint | 1 / 1 | 68 / 61 | 367,817 / 175,728 | $0.419318 / $0.247714 | 742 / 807s |
| IPython | 0 / 1 | 60 / 54 | 244,425 / 139,545 | $0.254152 / $0.181203 | 803 / 929s |

Totals: Harbor 1/2 versus 2/2, calls 128 versus 115, uncached input 612,242 versus 315,273 (-48.5%), and cost $0.673470 versus $0.428917 (-36.3%). Sum of active task wall rose 1,545s to 1,736s (+12.4%). Both arms' tasks ended locally `blocked` despite Harbor passes: the independent local verifier did not admit unsupported completed-evidence claims, while Harbor judged the submitted workspace. These are consumed diagnostic tasks and one attempt each; quality gain and cost/token saving are live observations, not a general promotion estimate. IPython baseline itself varied from the earlier pass, confirming substantial stochastic uncertainty.

After the first navigation boundary, mapped `fs.read` calls fell 25→22 on actionlint and 16→15 on IPython. Physical source-read lines fell 3,082→2,707 and 2,122→1,504, respectively. **Same-version overlap requests rose** 122→401 and 95→189. Actionlint had four fully covered repeat requests: `config.go` 1–161, `linter.go` 45–119, `command.go` 125–189, and `linter.go` 120–219. Three were fully reacquired; one was also read from source. The worker epoch changed after a no-effect parallel-call exception; the PTC catalog was keyed to that old epoch, although the later file SHA matched. The 224-sequence navigation snapshot showed only four newest detailed capture recipes under the 3,000-token budget, omitting several older whole-file paths. This is a real remaining reread failure masked by lower total task work.

The corrected review handoff did remove E2's immediate-block failure: IPython had one review yield, then a 3,003-byte decision packet and a `verify` response without a rejected tool; the local verifier rejected its premature no-edit claim and routed implementation. Actionlint's later decision packet was 3,397 bytes. First full navigation exposures and worker-loss reset packets are still 11–25KB; bounded patches can become large again when new evidence/events arrive. Do not infer per-call savings solely from packet labels.

Local verification failures need a separate receipt audit. Actionlint's sequence-261 `go test ./...` ended with exit code 1 because sandbox network was unreachable for unrelated tests. The managed receipt has a completed result and identical before/after workspace fingerprints, but old effect classification marked the failed shell attempt and containing cell `unknown`, preventing local verification. This is a harness accounting issue, not a Harbor reward failure. The new shared adapter classification marks only a single completed validation command with known exit code and identical workspace fingerprint as an observed failed attempt; status remains error, so it cannot be treated as a passing check. Any timeout, unknown effect, workspace change, or complex shell stays unresolved. Non-Git workspace fingerprints now hash workspace files rather than returning a constant empty digest.

## E4 frozen correction validation: complete, not promotable

Common implementation changes: receipt/fingerprint-grounded failed-validation effect classification. Treatment-only changes: `notebook_ptc.cross_epoch_read_reuse: true` (task-scoped, current-SHA-probed read catalog) and `work_batch_navigation@9` with a ≤3,200-byte exact path/SHA/range/artifact coverage index ahead of detailed recipes; capture-list deltas carry only added/removed entries. The baseline memory profile retains its existing epoch-keyed catalog and `@7` full navigation. The two checked-in profiles otherwise share the same implementation and model limits.

Freeze two diagnostic coding pairs before dispatch: `actionlint-action-pinning-lint` (known worker reset, same-version reacquisition, failed `go test`) and `ipython-session-bundle-replay` (review/evidence nonregression), OpenRouter Luna/max, same DeepSWE 1.1 confirmation manifest, one attempt/zero retries, concurrency four, same 32,768 output and 1B task-input limits. If no worker reset or matching repeated read occurs in actionlint, report the catalog mechanism unexercised rather than crediting a task-level reduction. Stop expansion on a Harbor quality regression, false completed-evidence admission, unresolved-effect bypass, or corrupt navigation replay. Compare post-first-navigation mapped requests, fully/partly-covered same-version ranges, physical source-read lines, catalog reused lines, exact recovery use, local verifier outcome, reward, calls/cost/tokens/wall, and packet bytes.

- Baseline: `/Users/mathiasl/skein-eval-results/delta-deepswe-e4-baseline-20260914`
- Treatment: `/Users/mathiasl/skein-eval-results/delta-deepswe-e4-delta-20260914`

| Task | Harbor reward baseline / treatment | Model steps | Uncached input tokens | Cost | Active wall seconds | Post-navigation read requests | Physical source-read lines |
| --- | --- | ---: | ---: | ---: | ---: | ---: | ---: |
| actionlint | 1 / 0 | 69 / 70 | 374,110 / 178,094 | $0.410183 / $0.259904 | 647 / 756 | 40 / 23 | 3,578 / 3,460 |
| IPython | 0 / 0 | 77 / 74 | 204,748 / 147,261 | $0.293335 / $0.231885 | 876 / 910 | 18 / 24 | 2,663 / 2,144 |

Totals: reward 1/2 → 0/2, steps 146 → 144, uncached input 578,858 → 325,355 (-43.8%), cost $0.703519 → $0.491789 (-30.1%), active wall 1,523 → 1,666 seconds (+9.4%). This is a used two-task diagnostic, not a quality/cost promotion estimate. Actionlint's treatment reward zero is a genuine official Harbor outcome. A network-disabled disposable replay with its exact `artifacts/model.patch` and local verifier image reproduced the hidden-suite build error: `rule_action_pinning_edge_test.go` referenced `pinningStep` and `runPinningStep` in the model's `rule_action_pinning_test.go`; the verifier's `test.patch` replaced that file, removing those helpers. The agent had repaired an earlier `config.go:167` syntax error and later passed a focused package test and build. The score-zero cause is the submitted test-file dependency across the official test overlay, not proof that memory damaged production source. IPython yielded the same one hidden magic-status failure in both arms (45/46 tests passed); neither arm passed quality.

The physical-reuse mechanism did fire in IPython: treatment receipt sequences 160, 166 and 234 reused 170, 42 and 250 source lines from artifact URIs whose original reads belonged to a different worker epoch. Thus **462 cross-epoch lines were reused live** with current-SHA probes. Treatment actionlint lost its worker once but had no same-version pre-reset capture requested afterward; its initial post-reset source files had changed SHA, so cross-epoch gain there is unexercised. IPython treatment read *requests rose* 18 → 24, despite physical fetch falling 2,663 → 2,144; neither reduced reasoning calls nor model avoidance of repeat requests is established. Baseline/treatment worker-epoch counts were actionlint 3/2 and IPython 4/2, which also confounds task-level fetch totals.

The shared E4 receipt fix had an unsafe live grounding bug: in Harbor, managed tools fingerprinted the local empty shadow workspace, not the remote `/app` where edits/commands occurred. Managed receipts for edits showed `workspace_before == workspace_after == e3b0c442...`, even though remote content hashes changed. A failed shell result classified `effect: observed` on that false empty-shadow equality; it remained an error and did not support completion, but the reconciliation premise was wrong. A common correction now injects the actual `ExecutionRuntime.repository` into managed tools and fingerprints that repository before/after claimed mutations. Deterministic remote-versus-shadow tests require a nonempty remote fingerprint, differing hashes after a remote mutation, and unresolved effect when it changes. Treatment flags remain opt-in pending a new live safety check.

## E5 frozen post-fingerprint live safety and diversity check: complete, negative quality/rereads

Paired DeepSWE 1.1 tasks previously absent from local result paths: `fastapi-implicit-head-options` (Python, medium feature) and `kcp-go-multiplexed-kcp-streams` (Go, easy feature). Baseline is the current memory profile; treatment is only the delta/cross-epoch profile. Both receive the corrected shared remote-repository receipt boundary. OpenRouter Luna/max, one attempt/zero retries, same pinned confirmation manifest, 32,768 output reserve, 1B task-input ceiling, four concurrent trials. Freeze source after full unit/integration/static checks. Required live safety evidence: managed receipts must carry a non-empty-shadow fingerprint for remote `/app`; remote edits must change it; only a completed failed validation with equal *remote* fingerprints may be an observed failed attempt, never a passing validation. Compare Harbor reward and local completion, model steps, uncached/cache/output tokens, provider cost, active wall, read requests, physical source lines, same-version repeat coverage, and any cross-epoch catalog reuse. If no matching worker-loss read occurs, catalog value remains unexercised. Stop expansion on false known-effect, quality regression, or newly enlarged uncached context without a compensating outcome.

- Baseline: `/Users/mathiasl/skein-eval-results/delta-deepswe-e5-baseline-20260915`
- Treatment: `/Users/mathiasl/skein-eval-results/delta-deepswe-e5-delta-20260915`

| Task | Harbor reward baseline / treatment | Model steps | Uncached input | Cached input | Output | Cost | Active wall seconds |
| --- | --- | ---: | ---: | ---: | ---: | ---: | ---: |
| FastAPI | 0 / 0 | 69 / 93 | 341,043 / 229,445 | 8,185,117 / 11,148,123 | 50,091 / 75,667 | $0.372067 / $0.371110 | 713 / 1,078 |
| KCP | 1 / 0 | 48 / 36 | 73,171 / 71,543 | 2,808,370 / 1,895,855 | 47,451 / 43,984 | $0.131394 / $0.108578 | 782 / 652 |

Totals: official reward 1/2 → 0/2, model steps 117 → 129 (+10.3%), uncached input 414,214 → 300,988 (-27.3%), cached input 10,993,487 → 13,043,978 (+18.7%), output 97,542 → 119,651 (+22.7%), provider cost $0.503461 → $0.479688 (-4.7%), active wall 1,495 → 1,730 seconds (+15.7%). Lower treatment cost with worse reward is not an efficiency win. All four local workflows remained blocked under independent evidence/recovery checks; Harbor judged their submitted workspaces separately.

The shared receipt safety correction is live-confirmed: initial FastAPI receipts in both arms held remote digest `dbe0b406...`, KCP held `fc31c06a...`, not E4's empty-shadow `e3b0c442...`. Treatment KCP's `write` of `mux.go` changed the before/after remote digest `fc31c06a...` → `fff3fe23...`. Thus known-failure classification can now be grounded in the workspace actually mutated. This does not resolve the genuine timed-out KCP command: both arms correctly failed closed on an unknown effect.

FastAPI is negative for model-level rereads. After each arm's first navigation event (sequence 100 baseline / 108 treatment), completed `fs.read` requests rose 57 → 129, returned source lines 17,013 → 42,388, same-version pre-cut overlap 3,108 → 7,697, and catalog-served lines 1,817 → 28,460. Physical source fetch fell only 15,196 → 13,928 (-8.3%). Treatment had four worker epochs versus baseline three. Three treatment cells rebuilt near-whole `fastapi/applications.py` and `fastapi/routing.py` page dictionaries after execution exceptions at sequences 364 (`error`), 428 (`AssertionError`) and 494 (`NameError`) reset the worker. The repeated page reads were PTC-catalog served: 64 read requests consumed **at least 24,275 lines from artifacts captured in earlier worker epochs**. The model still received those lines again, so calls, output and wall worsened. The exact coverage index did not persuade it to recover previously assembled values, and the committed state was not available after a reset. A treatment verification attempt at sequence 722 was correctly rejected for no completed passing behavioral check and nine unresolved execution records. This is a live representation/recovery failure, not a physical source-I/O failure.

KCP had little read-reuse opportunity: across each whole task (because baseline emitted no navigation event), baseline/treatment read requests were 5/4 with no same-version overlap. Harbor's treatment score zero was 28/30 new tests passed plus all 12 existing tests passed; the two failures checked removal from each session map after both sides closed. Treatment `handleClose` canceled its pending reciprocal close frame, while baseline explicitly sent that frame, explaining the peer-close state divergence. Both workflows stopped locally after a timed-out full-suite operation left an unknown effect, which the harness correctly fenced. This is a task/protocol implementation choice; there is no evidence that catalog reuse caused it. The accepted lifecycle criterion was explicit in the task (`remove only after both close and buffered data drains`), but treatment's stored next action and review did not turn that into a decisive two-peer check before its terminal unknown. Do not patch the task or weaken unknown-effect recovery as a memory fix.

## E6 frozen diagnostic: committed plain-value recovery after worker reset

The E5 FastAPI trace makes this a focused mechanism diagnostic, not held-out quality evidence: three worker-loss exceptions caused repeated near-whole-file paging. Treatment captures bounded JSON-plain successful-cell values and exact omitted names in a private content-addressed artifact, then verifies event/task/notebook/cell/hash/program identity and restores only those historical values after a worker reset. The prompt names restored bindings and their shape. Unknown effects and independent completion gates remain separate fences. Existing snapshot mode is not promoted. Focused reset/changed-source, unsupported-value, tamper, context, unit, integration and static checks must pass before paid launch.

Freeze one OpenRouter Luna/max DeepSWE 1.1 `fastapi-implicit-head-options` diagnostic against E5's *old delta* result: identical pinned manifest, one attempt, zero retry, 32,768 output reserve, 1B task-input ceiling; treatment is `notebook-ptc-memory-delta.yaml` with opt-in `recover_committed_values`. Path planned: `/Users/mathiasl/src/skein/.artifacts/live-e6-fastapi-20260915`. This is not a same-time randomized quality pair, and attribution will rely on checkpoint/restore events and same-version model-visible repeated lines, not just task-level score. Compare official Harbor reward, local safety outcome, restored bindings vs omitted names, worker resets, post-reset `fs.read` count/lines, physical/catalog lines, model steps, uncached/cached/output tokens, cost, and active wall to E5 delta. Stop this route on false state/freshness, no live recovery opportunity, no model-visible reread reduction, or worse calls/quality. If the mechanism works, run a fresh diverse paired confirmation cohort before considering any default promotion. The KCP task's review/proof-obligation failure is a separate follow-on direction, not silently bundled into this state-recovery trial.

E6 completed at `/Users/mathiasl/src/skein/.artifacts/live-e6-fastapi-20260915`; the trial JSON and event ledger are under `001-fastapi-implicit-head-options-attempt-01/001-fastapi-implicit-head-options-attempt-01/f871b4d12ae65f2c2a011dc321be399c__Vo27yyV/`. Full unit/integration, focused corruption/reset/changed-source tests, ruff and pyright passed before dispatch. The same task's E5 old-delta result is `/Users/mathiasl/skein-eval-results/delta-deepswe-e5-delta-20260915`.

| FastAPI diagnostic: old delta → E6 | E5 | E6 |
| --- | ---: | ---: |
| Official Harbor reward | 0 | 1 |
| Model calls | 93 | 102 |
| Uncached input tokens | 229,445 | 228,457 |
| Cached input tokens | 11,148,123 | 12,902,386 |
| Output tokens | 75,667 | 95,221 |
| Provider-equivalent cost | $0.371110 | $0.429412 |
| Active task wall | 1,078s | 1,246s |
| `fs.read` requests after first navigation | 129 | 87 |
| Source lines returned after first navigation | 42,388 | 29,067 |
| Catalog-served lines after first navigation | 28,460 | 19,112 |
| Physical source lines after first navigation | 13,928 | 9,955 |

E6 has four actual `repl.state_restored` events at sequences 416, 483, 514 and 550. The checkpoint restored historical plain values without accepting them as fresh or completion evidence; local completion still blocked on no successful behavioral verification and unresolved effects. Overall returned lines fell 31.4% and physical lines 28.5% versus E5's single older attempt, but calls, cached input, output, cost and wall rose; one-task stochastic quality 0→1 is not causal evidence. Reliability still fails at the decisive transition: an `AssertionError` at sequence 407 reset the worker, navigation at 413 advertised only the first 25 alphabetical checkpoint names and omitted `routing_text`, `routing_text_exact` and `routing_plan`, and restoration at 416 returned a truncated 64-name state manifest despite more selected values. The next submitted cells paged all 4,952 lines of unchanged `fastapi/routing.py` again (read events 435–456; source SHA `07c7a5bd...`). After the first restoration, 51 read requests returned 16,479 lines, of which 15,376 overlapped earlier reads of the same path/SHA. The checkpoint value `routing_text_exact` hashes to that exact source SHA, so a grounded binding plus a one-line current-SHA check could have avoided whole-file model-visible re-egress. The model's note said the complete snapshot was retained but directed recovery “as needed”; the prompt did not name the relevant binding. E6 is a partial task-level read reduction, not reliable memory use or a default-promotion result.

## E7 proposed correction from E6: grounded, utility-ranked historical bindings

Keep E6's successful-cell checkpoint and unknown-effect fence. Do not index the first N alphabetical values: report all actually restored names (bounded checkpoint selection with exact omissions), prioritize exact source-content bindings and large source/plan values within the prompt budget, and attach a source path/SHA only when a successful completed `fs.read` receipt's source SHA equals the checkpointed string's content SHA. Include the read-event identity and historical/freshness status; no path heuristic or stale-source promotion. The continuation must tell the model the exact variable to use and require a cheap one-line current SHA read before using its historical full text for edits; if the SHA differs, reacquire changed source. Verify the binding/provenance projection deterministically, preserve event replay hashes and completion fences, then run a same-time E6-versus-E7 live pair on the used FastAPI mechanism task plus a previously unused diverse task. Score post-reset *model-visible* same-SHA read lines, calls/cost/quality/safety; no default promotion on a one-task pass or a source-label mismatch.

E7 implementation is opt-in through `notebook_ptc.ground_committed_bindings`; it requires `recover_committed_values`. The E6 plain-checkpoint route remains available as the control. Grounded summaries now rank content-SHA-matched completed source bindings ahead of large source/plan strings and ordinary variables, preserving exact variable names and read-event IDs. The restored-value RPC returns one root descriptor per selected value, rather than the ordinary `agent.state.list()` 64-item browsing cap. The current-SHA probe remains a real repository read; a mismatched source version is reacquired, not trusted from history. Deterministic ranking, >64 restored-name identity, changed-source SHA, navigation message, corruption/reset, full suite and static preflight are required.

Frozen E7 simultaneous live pair: DeepSWE 1.1 `fastapi-implicit-head-options` (used mechanism diagnostic) and `tomlkit-toml-table-converters` (not mentioned in prior experiment records; new Python task-family confirmation), pinned confirm manifest SHA `f30a3bb6...`, OpenRouter `openai/gpt-5.6-luna` reasoning `max`, one attempt and zero Harbor retries per task, 32,768 output reserve, 1B task-input ceiling, two concurrent trials per arm (four total). Control is E6's archived composed profile at `.artifacts/live-e6-fastapi-20260915/001-fastapi-implicit-head-options-attempt-01/001-fastapi-implicit-head-options-attempt-01/f871b4d12ae65f2c2a011dc321be399c__Vo27yyV/agent/skein-state/evaluation/config.yaml` with grounded flag absent/false; treatment is the checked-in `notebook-ptc-memory-delta.yaml` with grounded flag true. The exact restored-name reporting correction is shared by both arms. Both dry plans selected the same two task IDs and manifest. Output roots: `.artifacts/live-e7-control-20260915` and `.artifacts/live-e7-grounded-20260915`. Compare official reward and local verifier outcome separately, current-SHA-check use, post-restoration repeated same-path/SHA returned versus physical lines, model calls and all token buckets, cost/active wall, source-label correctness and any new unknown-effect admission. Halt expansion on false source provenance, stale-source promotion, completion-fence bypass, or a fresh-task Harbor quality regression; an unexercised reset stays inconclusive on recovery.

E7 is complete. Both arms have the same pinned manifest and git diff SHA `7dab27f6...` in their `run-metadata.json`. Result and event roots are `/Users/mathiasl/src/skein/.artifacts/live-e7-control-20260915` and `/Users/mathiasl/src/skein/.artifacts/live-e7-grounded-20260915`; each task contains a Harbor `result.json` under its trial directory and an `agent/skein-state/runs/<run-id>/events/<task-id>.jsonl` ledger. The used FastAPI treatment ledger is specifically under `001-fastapi-implicit-head-options-attempt-01/001-fastapi-implicit-head-options-attempt-01/f871b4d12ae65f2c2a011dc321be399c__nvrwEfe/agent/skein-state/runs/ac2f35eebefd240f18a33a4fc77d4538/events/81b3ff59e50addce21131db628789eba450c613d3b810be2e087129db35a443b.jsonl`. Full unit/integration, focused source-rank/changed-SHA/>64-restoration/navigation checks, ruff, pyright and diff checks passed. Grounded checkpoints carried `committed_value_summaries@2`; controls carried `@1`. No new source-label mismatch or false local completion was observed; all four local outcomes remained `blocked` under independent evidence/reconciliation.

| E7 task / arm | Harbor | Calls | Uncached input | Cached input | Output | Cost | Active wall | Post-nav `fs.read` requests / returned / physical / catalog lines |
| --- | ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| FastAPI control | 0 | 96 | 204,945 | 11,440,834 | 72,086 | $0.366542 | 1,042s | 59 / 18,451 / 8,430 / 10,021 |
| FastAPI grounded | 1 | 144 | 268,835 | 22,379,796 | 107,441 | $0.643712 | 1,757s | 99 / 32,263 / 9,020 / 23,243 |
| Tomlkit control | 0 | 94 | 196,607 | 10,627,907 | 89,884 | $0.369557 | 1,150s | 29 / 6,142 / 3,107 / 3,035 |
| Tomlkit grounded | 0 | 86 | 198,057 | 10,462,189 | 89,703 | $0.366389 | 1,139s | 14 / 2,279 / 2,182 / 97 |

Totals: Harbor 0/2 → 1/2, calls 190 → 230 (+21.1%), uncached input 401,552 → 466,892 (+16.3%), cached input 22,068,741 → 32,841,985 (+48.8%), output 161,970 → 197,144 (+21.7%), cost $0.736098 → $1.010101 (+37.2%), and summed active wall 2,192s → 2,896s (+32.1%). The paired FastAPI quality 0→1 is observed but still one stochastic used task, not evidence that extra rereads caused quality. The previously unused Tomlkit task scored zero in both arms; treatment had no worker restoration, so its smaller task-level read/call count cannot qualify the recovery mechanism.

The FastAPI mechanism failed decisively. The grounded navigation at sequence 414 included correctly attested `app_source_exact` (`fastapi/applications.py`, SHA `77a6608b...`) and `routing_source_exact` (`fastapi/routing.py`, SHA `07c7a5bd...`). The model used both retained strings in a submitted `ast.parse` cell at sequence 438, with no immediate new read. That cell raised `AttributeError` at 439 and reset the worker. The checkpoint **did** restore 67 plain names, including both exact strings and matching historical source refs, at 441; no values or identities were missing. But the failure's model-visible result says "Live bindings were discarded. Recover applicable completed read artifacts", while auto-restoration is deferred until the *next* `execute_code` call. The model had already planned that next cell as three full application read batches followed by routing read batches; read events 448–503 alone returned both unchanged files to the model. After the first actual restoration, 60 read requests returned 20,729 lines, of which 19,288 were catalog-served from prior same-SHA captures; physical file lines were only 1,441 plus 50 one-line identity probes. Across the whole task, model-visible post-nav lines worsened 18,451 → 32,263 (+74.9%) while physical lines rose only 8,430 → 9,020 (+7.0%). The recovery representation worked; its timing and contradictory failure wording drove model-level re-egress and excessive reasoning calls. Do not promote E7 on a Harbor pass coupled to this cost/reread regression.

## E8 next distinct correction: restore and acknowledge before model replans

Move safe committed-plain restoration to the terminal path of a no-effect runtime failure, after appending the failed-cell event but **before** returning its model-visible tool result. Refactor the existing verified checkpoint restore function so start-of-next-cell and eager-after-failure use the same identity/hash/summary checks; never snapshot the failed calculation. The failure response must report the actual new worker epoch and restored high-utility binding names/source refs, state historical freshness and cheap current-SHA requirement, and explicitly say that unknown effects or failed validation are not reconciled. Avoid a generic "bindings discarded" directive when the bindings have in fact been restored. Skip eager restoration on timeout, current-cell unknown or known-changed effect, or unavailable/corrupt checkpoint; a source changed externally is still historical and requires the advertised current-SHA check. Keep fail-closed errors and old fallback behavior. Deterministic tests require read once → no-effect `AttributeError` → restored-value acknowledgement in that same failure response → next model-plannable cell uses the binding with no source reread, plus unknown-effect and corrupted-artifact controls. No default promotion until reduced same-SHA re-egress and safety hold in more than one task.

E8 implementation is opt-in through `notebook_ptc.eager_committed_restore` and requires `recover_committed_values`. The two restoration timings now share the same checkpoint/event/task/notebook/hash/program/manifest verifier; eager recovery appends `repl.state_restored` with `recovery_timing: after_failed_cell` before the failure response. The error text names only historical restored bindings and the actual new live epoch, and is redacted/bounded. A timeout with unknown effect stays non-live; a corrupt checkpoint raises during the failure call; neither is admitted as completed evidence. Focused integration controls, ruff, pyright and diff checks passed. The full unit/integration suite also passed (100%, exit 0) alongside the frozen live pair; its launch-time source hash remained matched across arms.

Frozen same-time E8 pair: used `fastapi-implicit-head-options` as the observed worker-reset mechanism diagnostic, plus previously unmentioned Go task `yaegi-go-embed-directives` for a diverse fresh quality check. Control is E7's archived composed profile at `.artifacts/live-e7-grounded-20260915/001-fastapi-implicit-head-options-attempt-01/001-fastapi-implicit-head-options-attempt-01/f871b4d12ae65f2c2a011dc321be399c__nvrwEfe/agent/skein-state/evaluation/config.yaml` (`ground_committed_bindings: true`, eager flag absent/false); treatment is the checked-in delta profile with eager true. Both dry plans selected the same tasks and pinned DeepSWE 1.1 confirm manifest SHA `f30a3bb6...`. OpenRouter Luna/max, one attempt/zero Harbor retry, 32,768 output reserve, 1B task-input ceiling, two concurrent trials per arm (four total). Output roots: `.artifacts/live-e8-control-20260915` and `.artifacts/live-e8-eager-20260915`. Compare completed Harbor reward, local evidence status, whether an actual no-effect runtime failure fired, restoration-event timing, the error response and immediate next submitted cell, same-version full-file reread requests/returned lines after each reset, physical versus catalog lines, calls/token buckets/cost/active wall. The Go task is a fresh generalization check; if it never has a recoverable reset, report the recovery mechanism unexercised there. Stop expansion on false restoration/freshness, unknown-effect bypass, quality regression or model-visible reread/call growth; do not attribute one used-task quality swing to the recovery timing.

E8 in-flight trace observation (not a terminal outcome): eager FastAPI restored a plain checkpoint at event sequence 109 before the next submitted cell; eager Yaegi restored at sequences 316 and 321 after two no-effect failed cells. The second Yaegi restoration produced live epoch `0d403905eff843d380b873f2d56a75c7`, but `context.work_packet_created` sequence 328 recorded the previous failed-cell epoch `9b465dbe08f9473395700821229349e1`. `app/agent/workflow.py` selects the latest epoch only from completed/failed/timeout cell events, excluding later `repl.state_restored`. This caused a second unnecessary full `worker_epoch_changed` packet at sequence 510 (23,495 bytes) after the first full packet at 328 (20,793 bytes); the subsequent packet at 524 was a normal patch (2,337 bytes). The paired Yaegi control recorded one full worker-transition packet at sequence 264, then a patch at 398. This is a distinct trace-backed E9 candidate; preserve E8 source until both arms finish, then repair epoch selection and validate in a fresh live pair if E8 read/call evidence is negative.

E8 is terminal; all four Harbor trials completed without trial errors or retries. Launch metadata in both roots carries the same source diff SHA `439029e7...`, manifest `f30a3bb6...`, OpenRouter `openai/gpt-5.6-luna`/max, and one attempt. All four local Skein completion results remained `blocked`/`verified: false`; Harbor rewards are independent. Eager restoration must not be promoted as a default on this evidence.

| E8 task / arm | Harbor reward (F2P; P2P) | Calls | Uncached / cached input | Output | Cost | Active wall | Post-first-nav `fs.read` requests / returned / physical / catalog / prior-same-SHA-overlap lines |
| --- | --- | ---: | ---: | ---: | ---: | ---: | --- |
| FastAPI control | 1 (43/43; 3134/3134) | 98 | 176,866 / 10,552,571 | 63,918 | $0.331955 | 1,118s | 13 / 2,970 / 1,341 / 1,629 / 1,629 |
| FastAPI eager | 0 (43/43; 3131/3134) | 99 | 196,291 / 11,801,535 | 67,388 | $0.365954 | 1,407s | 24 / 4,542 / 3,826 / 716 / 869 |
| Yaegi control | 0 (37/38; 58/58) | 85 | 176,375 / 9,897,845 | 59,580 | $0.313534 | 1,025s | 45 / 4,800 / 3,583 / 1,217 / 1,222 |
| Yaegi eager | 0 (2/38; 51/58) | 100 | 238,708 / 13,248,735 | 64,550 | $0.402097 | 1,356s | 57 / 6,910 / 5,319 / 1,591 / 1,631 |

Pair totals: Harbor 1/2 control → 0/2 eager; calls 183 → 199; uncached input 353,241 → 434,999; cached input 20,450,416 → 25,050,270; output 123,498 → 131,938; cost $0.645489 → $0.768051; summed active wall 2,143s → 2,763s. Model-visible post-nav read requests rose 58 → 81 and returned lines 7,770 → 11,452; physical source lines rose 4,924 → 9,145. Prior same-SHA overlap lines fell slightly in FastAPI and rose in Yaegi, so the broader acquisition and calls—not a uniform duplicate-overlap rise—drive the negative read conclusion. Counting from canonical completed `fs.read` events matched E7's independently recorded read totals as a cross-check; `read_reuse.reused_lines` distinguishes catalog-served from physical source lines. Yaegi eager's additional 23.5 KB full work packet is one demonstrated extra context cause, not proof it explains every added call or the different model patch.

Quality trace: FastAPI eager's verifier [CTRF report](../../.artifacts/live-e8-eager-20260915/001-fastapi-implicit-head-options-attempt-01/001-fastapi-implicit-head-options-attempt-01/f871b4d12ae65f2c2a011dc321be399c__VongLC4/verifier/ctrf.json) identifies three preserved OpenAPI callback schema snapshot failures (callback path operations became `{}`), while all 43 new feature tests passed. Yaegi control missed only `TestEmbedFSReadDirBatched` (`EOF` instead of `<nil>`); Yaegi eager's verifier output panicked during `TestEmbedFSSingleFile` at `/app/interp/type.go:1412`, leaving most new tests unrun and seven preserved tests missing. The eager Go patch changed eight paths rather than control's five. These are distinct model-code outcomes; the timing switch alone does not causally prove the quality regression, but it demonstrably did not protect quality in this paired live trial.

Recovery trace: eager FastAPI appended `repl.state_restored` at sequence 109 after a no-effect failed cell and before the next submitted cell, but the checkpoint then contained partial page bindings, not a complete exact-source string; subsequent uncovered-range reads cannot all be called duplicates. Later FastAPI `before_cell` restores at sequences 354 and 410 followed *known-changed* failed cells, so the eager safe-no-effect path correctly did not run there. Eager Yaegi's `repl.cell_failed` at 315 arose from model-generated `refs["read:3"]` despite `read:3` being absent from the map comprehension; after eager restore at 316, its submitted cell at 319 called `agent.state.cite("read:…")` for old-epoch live handles and failed at 320 with `KeyError("unknown retained read")`, triggering another eager restore at 321. The checkpoint restores plain values, not live retained-read handles, and the handoff does not offer the model a convenient historical artifact URI citation for those old handles. E9 should first fix the stale work-packet epoch and test whether unnecessary full context disappears; a separate, receipt-backed historical citation handoff is the next candidate if model-call/read growth remains. Do not make `agent.state.cite` silently treat stale handles as current live reads.

Exact result roots: `.artifacts/live-e8-control-20260915` and `.artifacts/live-e8-eager-20260915`; each task's nested `result.json` is the Harbor summary and `agent/skein-state/evaluation/result.json` is the independent Skein status. Canonical trace files are:

- Control FastAPI: `.artifacts/live-e8-control-20260915/001-fastapi-implicit-head-options-attempt-01/001-fastapi-implicit-head-options-attempt-01/f871b4d12ae65f2c2a011dc321be399c__NBPU2hz/agent/skein-state/runs/ac2f35eebefd240f18a33a4fc77d4538/events/81b3ff59e50addce21131db628789eba450c613d3b810be2e087129db35a443b.jsonl`.
- Control Yaegi: `.artifacts/live-e8-control-20260915/002-yaegi-go-embed-directives-attempt-01/002-yaegi-go-embed-directives-attempt-01/85d5af1af6e3a5abfdbd43a0c84171bc__ErAjNPA/agent/skein-state/runs/827c940df062cdaf0f01b05e153a751e/events/c5b795298ca72e7df1e6202ca6cb0206f6f7bfdb91cfae3da5942eca37762fc8.jsonl`.
- Eager FastAPI: `.artifacts/live-e8-eager-20260915/001-fastapi-implicit-head-options-attempt-01/001-fastapi-implicit-head-options-attempt-01/f871b4d12ae65f2c2a011dc321be399c__VongLC4/agent/skein-state/runs/ac2f35eebefd240f18a33a4fc77d4538/events/81b3ff59e50addce21131db628789eba450c613d3b810be2e087129db35a443b.jsonl`.
- Eager Yaegi: `.artifacts/live-e8-eager-20260915/002-yaegi-go-embed-directives-attempt-01/002-yaegi-go-embed-directives-attempt-01/85d5af1af6e3a5abfdbd43a0c84171bc__JsBFFdm/agent/skein-state/runs/827c940df062cdaf0f01b05e153a751e/events/c5b795298ca72e7df1e6202ca6cb0206f6f7bfdb91cfae3da5942eca37762fc8.jsonl`.

## E9 packet-epoch correction and fresh paired full-system check

Root correction in `app/agent/workflow.py`: both work-packet epoch selectors now route through one event-derived function that accepts the newest terminal `repl.state_restored` epoch as well as completed/failed/timeout cell epochs. This is unconditional correctness, not an ablation flag that leaves a known stale-worker projection available. One deterministic event-stream replay test covers failed old epoch → restored new epoch → unrelated receipt → completed new epoch. Targeted orchestration/PTC/context suites, ruff, pyright (0 errors, one existing warning), and diff checks passed. Preserve the verified restore and unresolved-effect/completion fences. E8's archived eager Yaegi trace is the historical mechanism control: two full worker-transition packets at 328/510 due to stale epoch. A fresh corrected run must emit no *second* full transition merely because the packet stored the pre-restore epoch. If no safe eager restoration occurs, this mechanism remains unexercised rather than counted as a fix.

Frozen E9 live design: compare the corrected delta memory/PTC profile to the current `notebook-ptc-jsonl.yaml` PTC control on the same two DeepSWE 1.1 tasks at the same time. `yaegi-go-embed-directives` is the used worker-reset/quality diagnostic; previously absent `psd-tools-blend-range-api` is a fresh, diverse Python quality/efficiency check. Both dry plans returned exactly those two tasks from confirm manifest SHA `f30a3bb6...`. OpenRouter Luna/max, one attempt/zero Harbor retries, 32,768 output reserve, 1B task-input ceiling, two concurrent trials per arm (four total). This paired comparison evaluates full-system value versus PTC control, **not** the isolated causal contribution of the epoch code fix: that fix is in both arms, while notes, recall, navigation and recovery differ by profile. Compare official Harbor reward, independent local evidence outcome, calls, token buckets/cost/wall, post-first-navigation same-SHA overlap versus new coverage, physical/catalog/model-visible read lines, runtime failures/actual restorations, and work-packet reset sequence/bytes. No default promotion on quality regression, increased source reacquisition or a single stochastic quality swing. Keep historical `agent.state.cite` handles unavailable across epochs; the receipt-backed historical citation handoff is a separate E10 candidate if E9 remains negative.

E9 live launch roots: `.artifacts/live-e9-ptc-control-20260915` and `.artifacts/live-e9-memory-corrected-20260915`. Both run metadata records carry identical launch-time source diff SHA `56cd649f...`, manifest `f30a3bb6...`, Luna/max and one attempt; only the intended profile differs. The first launch attempts stopped before creating trials because the workspace sandbox denied access to the user-owned Colima socket, not because Docker was down; elevated `docker version` confirmed the server healthy and the two frozen commands were then dispatched with Docker access. No trial or provider result from the failed attempts was substituted into this cohort.

E9 terminal result: all four trials completed with zero Harbor errors or retries. All four independent Skein results are `blocked`, `verified: false` despite the Harbor rewards; no failed/unknown effect or model claim was promoted to verified completion. This is not a default-promotion result: quality tied at 1/2, but the memory arm increased calls, cost and physical source acquisition overall.

| E9 task / arm | Harbor (F2P; P2P) | Calls | Uncached / cached input | Output | Cost | Active wall | Whole-task completed `fs.read` requests / returned / physical / catalog / prior-same-SHA-overlap lines |
| --- | --- | ---: | ---: | ---: | ---: | ---: | --- |
| PSD Tools PTC | 1 (45/45; 979/979) | 59 | 173,834 / 4,783,176 | 47,025 | $0.195543 | 668s | 26 / 4,355 / 2,886 / 1,469 / 1,528 |
| PSD Tools memory | 1 (45/45; 979/979) | 76 | 191,483 / 8,044,323 | 60,138 | $0.280911 | 872s | 29 / 4,919 / 4,067 / 852 / 852 |
| Yaegi PTC | 0 (37/38; 58/58) | 60 | 131,887 / 5,132,715 | 40,208 | $0.183867 | 814s | 38 / 4,906 / 4,149 / 757 / 757 |
| Yaegi memory | 0 (37/38; 58/58) | 58 | 131,355 / 4,406,417 | 38,036 | $0.166602 | 790s | 40 / 5,432 / 4,352 / 1,080 / 1,080 |

Pair totals PTC → memory: Harbor 1/2 → 1/2; calls 119 → 134; uncached input 305,721 → 322,838; cached input 9,915,891 → 12,450,740; output 87,233 → 98,174; cost $0.379410 → $0.447513; summed active wall 1,482s → 1,662s. Whole-task read requests rose 64 → 69, returned source lines 9,261 → 10,351, and physical source lines 7,035 → 8,419. Prior same-SHA overlap lines fell 2,285 → 1,932 overall, but the reduction occurred on PSD Tools (1,528 → 852) while Yaegi worsened (757 → 1,080); different source coverage and task trajectories prohibit claiming reliable model avoidance of rereads. PSD memory's 17 added calls and $0.085 higher cost overwhelm Yaegi memory's two-call/$0.017 gain. PSD memory median provider input was about 113k tokens versus PTC's 83k, mostly cached because its longer tool/history trajectory grew the prompt; uncached per call was not uniformly higher.

Mechanism and failure trace: E9 memory restorations occurred at PSD sequence 217 and Yaegi sequence 460, both `before_cell` after known-changed or external worker-loss boundaries; no `after_failed_cell` eager recovery fired. PSD's sequence 322 full worker-transition packet correctly recorded the restored new epoch and later packets stayed patches, but the specific E8 two-eager-restores stale-packet path was unexercised. Yaegi memory never needed a worker-transition full packet in the observed work-batch sequence. Do not credit the unconditional E9 epoch fix with a live cost improvement from this pair; its event-stream regression check remains deterministic only.

PSD memory submitted 74 PTC cells versus control's 57, made 16 successful edits versus 7, had 15 failing shell operations versus 10, and emitted seven evidence-navigation packets/four working-note updates without a context cut. At event 214 a first `fs.edit` completed and changed `src/psd_tools/api/layers.py`; the model's next edit in the same cell read `layers_import_edit["data"]["sha256"]` at line 33, but the attested after-hash lived at `content_hashes[path]`, not `data.sha256`. `repl.cell_failed` 215 was `KeyError('sha256')` with `effect: changed`, so the worker reset and restoration correctly waited until the next call; no failed partial cell or unknown effect was considered verified. This is a concrete PTC result-schema/usability defect with a likely extra-reasoning cost, distinct from memory note or navigation overhead. The larger PSD memory implementation and local model-authored test edits are different model trajectories; the seven navigation packets and 17 extra calls correlate but do not alone establish causation. Keep the tool result contract explicit (structured after-SHA at an obvious path plus help recipe), and make memory navigation useful on intact workers without repeatedly reprinting bulky evidence already accessible through live PTC state. E8 Yaegi's stale live `read:`-handle citation failure remains a separate historical-receipt handoff candidate.

Exact canonical E9 trace paths:

- PTC PSD Tools: `.artifacts/live-e9-ptc-control-20260915/001-psd-tools-blend-range-api-attempt-01/001-psd-tools-blend-range-api-attempt-01/60ceb52ff33c3e5d78909917d048cc79__UYQhEq5/agent/skein-state/runs/e0e061ef1b816d8af454b4bd6bc2e8fa/events/b3ec8039afa730f4ac3c2bb4dfc22b221d0b027abd4530c19385dcf6c1c22f60.jsonl`.
- PTC Yaegi: `.artifacts/live-e9-ptc-control-20260915/002-yaegi-go-embed-directives-attempt-01/002-yaegi-go-embed-directives-attempt-01/85d5af1af6e3a5abfdbd43a0c84171bc__pttnFxT/agent/skein-state/runs/827c940df062cdaf0f01b05e153a751e/events/c5b795298ca72e7df1e6202ca6cb0206f6f7bfdb91cfae3da5942eca37762fc8.jsonl`.
- Memory PSD Tools: `.artifacts/live-e9-memory-corrected-20260915/001-psd-tools-blend-range-api-attempt-01/001-psd-tools-blend-range-api-attempt-01/60ceb52ff33c3e5d78909917d048cc79__sQNvQH4/agent/skein-state/runs/e0e061ef1b816d8af454b4bd6bc2e8fa/events/b3ec8039afa730f4ac3c2bb4dfc22b221d0b027abd4530c19385dcf6c1c22f60.jsonl`.
- Memory Yaegi: `.artifacts/live-e9-memory-corrected-20260915/002-yaegi-go-embed-directives-attempt-01/002-yaegi-go-embed-directives-attempt-01/85d5af1af6e3a5abfdbd43a0c84171bc__9vwsdQH/agent/skein-state/runs/827c940df062cdaf0f01b05e153a751e/events/c5b795298ca72e7df1e6202ca6cb0206f6f7bfdb91cfae3da5942eca37762fc8.jsonl`.

Each E9 task's nested `result.json` is its official Harbor trial summary; `agent/skein-state/evaluation/result.json` is its independent local evidence outcome, and `agent/skein-state/runs/<run-id>/metrics.db` plus `managed-tools.db` carry provider samples and managed-effect receipts. Whole-task completed-read aggregation was required because the PTC profile does not emit the delta arm's first-navigation event; the same event/`read_reuse` aggregation reproduced E7's already recorded results before E9 values were accepted.

## E10 bounded-navigation ablation with common PTC result repair

The E9 PSD memory trial's seven navigation snapshots each had an approximately 11.7 KB/2,900-token full view; only the first was exposed while the notebook was simply live, but six later views carried unchanged effect-reconciliation controls and changing advisory material. This repeated delivery correlates with 17 extra model cells/calls and higher cached prompt size; it does **not** prove the view caused the larger code/test trajectory. A deterministic replay of E9's recorded PSD and Yaegi navigation inputs through the existing `render_handoff` at 1,800 tokens produced 6.9–7.1 KB full views, retained required metadata and selected notes, and did not raise a context-budget error. A captured-read index still fit on Yaegi and the initial PSD view. Reducing the existing `compaction_tokens` would also reduce actual cut reserves, so E10 introduces a separate optional navigation budget while leaving compaction unchanged.

Common PTC contract repair: both `fs.edit` and `fs.write` should return the broker-attested after-SHA at `data.sha256` and `content_hashes[path]`, with `data.path` and a precise `agent.help` recipe. This makes a safe next `expected_sha256` edit chain executable; E9 PSD's `KeyError('sha256')` arose from the absent structured after-SHA. The top-level hash/effect/receipt remains authoritative; no model-supplied digest is accepted as a fact. Apply the common repair to both E10 arms before freeze so the one independent treatment variable is the navigation budget, not a PTC API disparity. Keep ordinary shell failures and unknown effects fail-closed; do not auto-reconcile the E9 missing-dependency or disabled-network shell receipts from workspace equality alone.

E10 control uses the checked-in delta memory profile at its existing navigation budget (fallback `compaction_tokens: 3000`); treatment differs only by `navigation_tokens: 1800` in a new opt-in profile. Preserve working notes, prior-run recall, phase-boundary compaction, exact read catalog, snapshot recovery, and independent verification identically. Deterministic checks must prove: after-SHA fields agree with actual bytes on first edit, write and idempotent replay; expected-hash chaining succeeds; both profiles keep the same compaction reserve and replayable prompt hashes; required unresolved-effect metadata survives the smaller navigation budget, while oversize critical metadata still fails closed. Freeze DeepSWE 1.1 confirm tasks `psd-tools-blend-range-api` and `yaegi-go-embed-directives` as used trace diagnostics plus previously absent TypeScript `happy-dom-deterministic-intersectionobserver` as diverse fresh quality check. Run Luna/max through authorized OpenRouter, one attempt/zero Harbor retries, 32,768 output reserve, 1B task-input ceiling, three trials per arm/six concurrent only after checks; compare Harbor reward, independent evidence status, calls/cached/uncached/output/cost/wall, actual provider request bytes, note/recovery use, whole-task same-SHA overlap and new-source lines, navigation exposure bytes and cuts. Do not promote on quality regression, lost evidence/control entries, or a cost/read gain limited to one used task. If the smaller view cannot beat E9-style cost without losing learned-value use, diagnose historical citation and active-worker handoff targeting next rather than repeatedly lowering budgets.

E10 launch record (2026-09-15 UTC): focused context, environment/tool, orchestration, PTC integration and configuration tests passed; Ruff, compileall, targeted Pyright and `git diff --check` passed. Both frozen task plans selected Happy DOM, PSD Tools, and Yaegi with manifest SHA `f30a3bb6245a360820be5684ed98d001cea460169168633c18487ef28d48fad4`; both launch metadata recorded source diff SHA `6d40e517f7ef647d5a452182f89a3e8a3170847a71b286f84480deaa99ae3321`. Control result root: `.artifacts/live-e10-nav3000-20260915`; treatment root: `.artifacts/live-e10-nav1800-20260915`. The first parallel-launch attempt was reviewer-rejected for unspecified disclosure scope, but one treatment launcher actually started before the rejection surfaced. Exact manifest/task files showed pinned cached public GitHub tasks and the key was used as provider authentication, so the scoped rerun was accepted. The first treatment launcher was interrupted after the duplicate was detected; its three `attempt-01` entries are incomplete and **must be excluded**. The accepted pair is control `attempt-01` and treatment `attempt-02`, three tasks each; only their six containers remain. First navigation full views were about 11.6–12.0 KB control versus 6.9–7.1 KB treatment; required metadata survived. These are in-flight mechanism observations, not terminal quality/cost/read evidence. No default promotion before completed paired traces.

E10 terminal results (all six intended Harbor trials complete, no Harbor errors/retries; control `attempt-01`, treatment `attempt-02` only):

| DeepSWE 1.1 task | Arm | Harbor reward | Model calls | Uncached input | Cached read | Output | API-equivalent cost | Active wall | Completed `fs.read` requests | Physical source lines | Catalog-served repeat lines |
| --- | --- | ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| PSD Tools | Navigation 3000 | 1 | 68 | 181,053 | 6,202,428 | 60,375 | $0.241752 | 875s | 25 | 2,988 | 1,187 |
| PSD Tools | Navigation 1800 | 1 | 67 | 144,816 | 5,167,659 | 52,360 | $0.202379 | 707s | 22 | 2,710 | 582 |
| Yaegi | Navigation 3000 | 0 | 48 | 128,360 | 3,879,529 | 42,513 | $0.160689 | 731s | 46 | 4,368 | 1,803 |
| Yaegi | Navigation 1800 | 0 | 44 | 126,155 | 3,274,494 | 34,631 | $0.138579 | 619s | 28 | 3,976 | 21 |
| Happy DOM (fresh task) | Navigation 3000 | 0 | 47 | 142,624 | 3,893,370 | 54,300 | $0.178676 | 903s | 34 | 3,267 | 397 |
| Happy DOM (fresh task) | Navigation 1800 | 0 | 66 | 166,087 | 6,783,119 | 58,790 | $0.247722 | 949s | 28 | 3,495 | 180 |
| **Total** | **Navigation 3000** | **1/3** | **163** | **452,037** | **13,975,327** | **157,188** | **$0.581117** | **2,509s** | **105** | **10,623** | **3,387** |
| **Total** | **Navigation 1800** | **1/3** | **177** | **437,058** | **15,225,272** | **145,781** | **$0.588681** | **2,275s** | **78** | **10,181** | **783** |

All six local Skein completions were `blocked/verified:false`; Harbor independently found PSD's passing patch in both arms. Navigation did not create a context cut in any of the six traces; this is a work-batch navigation-size ablation, not a compaction measurement. The treatment reduced completed read requests 25.7%, physical source lines 27.9%, and catalog-served repeated lines 76.9%, with unchanged 1/3 quality. It **did not** reduce calls or cost overall: calls +8.6%, cached input +8.9%, cost +1.3%, despite active wall -9.3%. The smaller budget is an opt-in candidate for source-egress reduction, not a default-promotion or reliable-memory-use proof. Two used tasks got cheaper and faster; the previously unused Happy DOM task's $0.178676→$0.247722 and 47→66 calls erased the aggregate cost gain. PSD treatment emitted 41.6 KB total navigation content versus 37.7 KB control because it produced more packets, so a packet-size-only causal explanation is false.

Happy DOM trace diagnosis: treatment `attempt-02` produced 15 unresolved effects at its sequence-315 navigation, versus six in control at sequence 394; later `effect_reconciliation_required` views remained 6.8–7.1 KB. Four treatment `npm run --workspace happy-dom test -- --run ...` failures (sequences 171, 245, 281, 299) were repeated during actual model patch/test revisions; the first had a TypeScript private-name compile error, later ones had two Vitest assertions failing. The managed-tool receipts report concrete `exit_code=1` and **equal before/after workspace fingerprints**, yet the receipts remained `failed` with unknown effects. Directly testing `is_reusable_validation_command` showed it accepts `npm test` but rejects the common `npm run --workspace happy-dom test` ordering; `_mutate` therefore cannot apply its existing narrow workspace-stable failed-validation settlement. A failed lint command and a later 120-second full-test timeout remain separately unresolved; neither can be declared safe from fingerprint equality alone. Treatment also had one no-effect failed PTC cell at sequence 348 and one unknown-effect timeout at 527; control timed out at 454. This is a concrete validation-command grammar/usability issue that amplifies unknown-effect navigation, but it does **not** prove the grammar caused the 19 extra Happy DOM calls: many reruns followed genuine model-authored code/test failures. Next canary should repair only the existing recognized test-command flag order, preserve the exit-code/fingerprint/no-metacharacter gate, and live-compare on Happy DOM plus a distinct control before widening the command policy or changing memory defaults.

## E11 workspace-test receipt canary

E11 changes only the existing recognized validation-command grammar to accept `npm run --workspace <package> test` and `npm run --workspace=<package> test`; the broker's concrete nonnegative exit code, unchanged execution-workspace fingerprint, no-metacharacter command, no existing unknown effect, and exact receipt identity gates remain in place. A failed test is a completed *failed check*, not passing verification evidence; changed workspace, arbitrary `npx` lint, timeouts, and unknown external effects remain unresolved. Unit checks cover recognition and both stable/changed workspace receipts. This repairs the Happy DOM E10 sequence-171/245/281/299 diagnostic without promising to fix the model's TypeScript patch or Vitest assertions.

Run a frozen provider-backed Luna/max canary using the same `navigation_tokens: 1800` memory profile on Happy DOM (mechanism diagnostic), PSD Tools (previously passing no-regression reference), and previously unused Arktype (new TypeScript repo). One attempt, zero retries, 32,768 output reserve, 1B task-input ceiling, three concurrent trials. E10 treatment `attempt-02` supplies a historical live comparator for Happy DOM and PSD Tools; it is **not a simultaneously randomized control**, so single-task model-trajectory differences are hypotheses, not causal proof. Arktype has no matched baseline and tests generality only observationally. The canary has to show that workspace-stable failed `npm run --workspace ... test` receipts actually settle as known, unresolved-effect views shrink, quality/evidence gates do not regress, and total calls/cost/read behavior plausibly improves. If classification never fires or calls stay high because real tests keep failing, stop broadening reconciliation and investigate model-authored code/test trajectories separately from memory navigation. Preserve E10's read-catalog and completion improvements regardless.

E11 live launch (2026-09-15 UTC): `.artifacts/live-e11-npm-workspace-20260915`, source diff SHA `0e00abc2ebc0beb03b48569b08c6cfd3c60a3976b0756764146c3cd5761b7480`, manifest SHA `f30a3bb6245a360820be5684ed98d001cea460169168633c18487ef28d48fad4`. The plan selected Arktype, Happy DOM, PSD Tools in that order. Focused verification/managed-adapter/context/tool tests, Ruff and targeted Pyright passed before launch. Three provider-backed trials are in flight; no terminal result should be inferred from the presence of a live `result.json`. Compare only terminal Harbor `result.json`/`runs.jsonl` and canonical run events.

E11 terminal trace audit (three Harbor trials complete, no Harbor errors/retries): PSD Tools reward 1, 69 calls, 153,466 uncached input, 6,268,918 cached read, 64,662 output, $0.241329, 819s active wall; Happy DOM reward 0, 62 calls, 144,589 uncached input, 5,048,241 cached read, 54,215 output, $0.202161, 853s wall; new Arktype reward 1, 92 calls, 224,200 uncached input, 11,993,433 cached read, 78,805 output, $0.390471, 1,286s wall. All three local completions remained `blocked/verified:false`. Happy DOM had 29 completed `fs.read` requests, 454 physical source lines and 46 catalog-served repeat lines; PSD Tools 18, 184 and 90; Arktype 27, 0 and 264 under the read-catalog receipt fields (do not interpret Arktype's zero as zero total source acquisition through all routes). Against E10's historical 1,800-token arms on the two repeated tasks, PSD cost worsened $0.202379→$0.241329 despite fewer reads, while Happy DOM cost improved $0.247722→$0.202161 with one more read request; Harbor quality tied on each. This reinforces that read reuse and model work/cost are separate, and historical single-attempt shifts are not causal.

The E11 mechanism was **not exercised live**: Happy DOM's submitted cells used `npm --prefix packages/happy-dom test`, which the preexisting grammar already recognizes, rather than `npm run --workspace happy-dom test`. A failed targeted check at canonical sequence 270 was safely `effect: observed` under the old contract; later lint at 389 and a timeout at 490 stayed unknown. E11 therefore provides deterministic proof of the widened grammar and a useful fresh Arktype quality observation, but **no live proof that widening reduced unknown-effect navigation or calls**. Do not promote that change or the smaller-navigation profile based on E11. Next experiment should target the root call/cached-context overhead independently of read-catalog reuse, with repeated paired diverse tasks or a direct provider-wire navigation exposure intervention; do not keep widening failed-shell reconciliation to address real model-authored test failures.

Read-metric correction (13:09 UTC): the initial physical-line aggregation above counted only `read_reuse.source_read_lines`, which is absent on many first reads. Recounting those completed `fs.read` events from `read_evidence.returned_lines` yields E10 physical lines 10,623→10,181 (4.2% reduction, not the 3,029→2,185/27.9% claim above), and E11 physical lines 2,312 Happy DOM, 2,917 PSD Tools, and 2,265 Arktype (not 454/184/0). For each task the corrected physical plus catalog-served repeat lines equals total returned lines; repeat-line and request counts above are unchanged. Historical claims in the preceding prose are superseded by these corrected numbers, not silently erased.

## E12 fresh six-task net memory-versus-PTC comparison

E10 demonstrated fewer rereads for smaller *memory* navigation while E9's older two-task memory-versus-PTC pair had higher calls/cost. E11's altered test-command branch was not used live and the single historic task shifts were mixed. No further code/config changes are justified before a current, genuinely diverse, provider-backed net-value comparison. E12 freezes six previously absent DeepSWE 1.1 confirm tasks: Python `httpx-streaming-json-iteration`, `narwhals-rolling-window-suite`; Go `anko-default-function-arguments`, `pebble-durability-wait-apis`; TypeScript `awilix-async-container-initialization`, `drizzle-orm-window-function-builders`. A bounded artifact-path search found no prior attempt directory for any of the six. Run the current one-tool PTC control profile against the current opt-in 1,800-token memory profile with shared source/after-SHA/validation-grammar fixes in both. One attempt, zero Harbor retries, Luna/max via authorized OpenRouter, 32,768 output reserve, 1B task-input ceiling, six total concurrent trials (three per arm) until all twelve are terminal; no selective retry or replacement.

Measure Harbor reward pairwise; local independent-evidence status; model calls, uncached/cached/output and provider-wire bytes, API-equivalent cost, active wall; completed source-read requests, physical lines, catalog-served same-SHA repeat lines, and missing/changed-source acquisition separately. Also inspect notes/continuation use, failed effects, worker restoration, phase changes/cuts and exact terminal reasons. A value claim needs no Harbor quality regression or false completed-evidence answer, repeat-read improvement on most paired tasks, and aggregate calls/cost no worse without a single outlier hiding a per-task regression. If memory lowers rereads but still raises calls/cached cost, diagnose provider-wire advisory exposure and model-authored test/patch loops as separate mechanisms before another implementation. Keep working notes, compaction and prior-run recall opt-in and do not promote this cohort by itself.

E12 launch (2026-09-15 UTC): both frozen `--plan` outputs selected the same six tasks in the same order; the two live arms use the same manifest SHA `f30a3bb6245a360820be5684ed98d001cea460169168633c18487ef28d48fad4` and source diff SHA `1d7537986c22e440f15ea40375f20418f86dbc37129a12122754f2d20b9a3cee`. PTC-control result root `.artifacts/live-e12-ptc-control-20260915`; memory result root `.artifacts/live-e12-memory-nav1800-20260915`. Both metadata files record Luna/max, one attempt, three concurrent each. All twelve live trials are in flight; no terminal quality or cost conclusion yet. Source/config are frozen until the pair completes. Record exact terminal trial result and canonical trace paths from each root's `runs.jsonl`; incomplete `result.json` snapshots are not outcomes.

E12 12:55 UTC checkpoint: four of six pairs are terminal, with no Harbor errors/retries. Awilix ties 0/0, Anko regresses PTC 1→memory 0, Drizzle and HTTPX tie 1/1. Narwhals and Pebble remain running/verifying and must not be replaced or selectively retried. Across the four completed pairs, cost is PTC $0.986778 versus memory $1.069079, while memory has fewer physical source lines on Anko (1,558→903), Drizzle (1,963→842) and HTTPX (1,046→770), tied Awilix (443→443). Completed `fs.read` requests are 10→15 Awilix, 32→34 Anko, 62→37 Drizzle, and 26→15 HTTPX: only two of four improve. This is already negative against quality/cost and majority read-request gates, even if remaining pairs improve aggregate metrics.

Anko's exact result files are `.artifacts/live-e12-ptc-control-20260915/001-anko-default-function-arguments-attempt-01/001-anko-default-function-arguments-attempt-01/e99368c8e599e7fc1687ac11dd92d6d9__jHLfwDf/result.json` and `.artifacts/live-e12-memory-nav1800-20260915/001-anko-default-function-arguments-attempt-01/001-anko-default-function-arguments-attempt-01/e99368c8e599e7fc1687ac11dd92d6d9__bcn3kfi/result.json`; both have null exceptions and local `blocked/verified:false`. Their verifier rewards are PTC 2/2 new and 119/119 preserved tests, memory 1/2 new and 119/119 preserved. The memory failure is `TestDefaultArgumentsVisible`: `ParseSrc error - received: syntax error - expected substring: invalid default argument declaration`. Both `artifacts/model.patch` files contain that phrase on some parser paths, but memory's submitted patch returns generic syntax error on the malformed declaration route at its parser lines 275–281. This is a real model-authored behavioral defect in the memory trajectory, not a Harbor setup failure or deterministic proof that a particular memory view caused it. Canonical run events and the verifier `reports/new-ctrf.json` under those exact trial directories remain the replay source.

E12 terminal pair (13:09 UTC): all twelve Harbor trials completed, no Harbor errors/retries. In task order Awilix, Anko, Drizzle, HTTPX, Narwhals, Pebble, PTC rewards were 0/1/1/1/1/1 (5/6) and memory rewards 0/0/1/1/1/1 (4/6). All twelve local Skein outcomes stayed `blocked/verified:false`, so no false verified-completion claim is credited. Agent-side totals: PTC 496 model calls, 1,118,328 uncached input, 47,307,780 cached read, 359,740 output, 246,625,809 provider request bytes, $1.657351 API-equivalent cost; memory 454 calls, 1,059,069 uncached, 47,492,391 cached, 365,436 output, 237,383,114 request bytes, $1.653070 cost. Memory therefore cut calls 8.5%, uncached input 5.3% and wire bytes 3.7%, but cost was effectively tied (-0.3%) and cached input increased 0.4%. These totals are agent-side completed result files; do not substitute summed trial wall for the concurrently elapsed batch wall.

Corrected completed `fs.read` metrics, in the same six-task order: PTC requests 10/32/62/26/73/51, physical lines 1,461/3,993/4,754/3,662/7,759/4,649, catalog-served same-SHA repeat lines 1,095/407/1,116/579/847/1,421; memory requests 15/34/37/15/72/61, physical lines 2,518/3,036/4,275/2,852/4,107/6,483, repeat lines 551/702/922/435/2,268/3,172. Totals: 254→234 requests (-7.9%), 26,278→23,271 physical lines (-11.4%), **5,465→8,050 repeat lines (+47.3%)**, and 31,743→31,321 returned lines (-1.3%). Three of six tasks increased read requests; Anko, Narwhals and Pebble increased same-SHA repeat lines. First reads without a `read_reuse` breakdown use `read_evidence.returned_lines` as physical acquisition; physical plus repeat equals returned per task. The 12:55 checkpoint's physical-line figures above used the incomplete aggregation and are superseded. This is physical-fetch progress, **not reliable model reuse** or a quality/cost win; the opt-in memory profile fails the prespecified quality and majority repeat-read gates. Freeze it without default promotion or selective retries. The canonical trace paths are under each terminal trial directory's `agent/skein-state/runs/*/events/*.jsonl`, indexed by each arm's `runs.jsonl`.

## E13 virtual-memory routing and fresh net-value gate

E12 negative mechanism, distinct from Anko's model-authored parser defect: Pebble's seq-83 PTC cell tried an evidence-linked five-entry `memory note write` with `cd /app && memory ...`; the nested shell returned exit 127, `memory: command not found`, and an unknown effect. Its five continuation packets at seq 149/260/355/542/614 all reported note version 0, and the first offered only three newest read artifact refs while omitting twelve earlier reads. Narwhals twice chained `memory query` after Git commands and got the same shell error. Factory wiring routes only a standalone command beginning `memory` through `ContextProgramService`; a compound command fell through to the ordinary shell. This is a real PTC/memory usability fault, not evidence that the model ignored a functioning recovery tool. Pebble's first 19 reads already had 1,052 catalog-served repeat lines before its first navigation, so work-packet exposure is not the sole repeat-read cause. The live worker retained epoch `4b88ca7002744821ac6672c00df2781a`; the model repeatedly printed overlapping `fs.read` results rather than consuming a retained binding. Do not attribute every overlap to avoidable rediscovery: narrow windows can be justified while implementing code. The E12 5,465→8,050 repeat-line regression is the stronger aggregate gate.

E13 correction group changes only the shared factory's virtual-command admission and stable PTC tool guidance: detect `memory` after a shell composition token, return a structured no-execution/effect-none response telling the model to call it alone, and state that `cd`, Git and other shell commands must be separate. Do not execute or reinterpret a compound memory command, add another top-level tool, widen shell authority, or weaken unknown-effect/verification gates. A standalone memory note/schema/query still follows the existing in-process service. Deterministic checks include quoted-data nonmatches, direct shared-function recognition, a real PTC cell that blocks the compound call and successfully routes a standalone schema call; focused factory/context/PTC tests, Ruff, compileall, targeted Pyright and diff check passed before a paid run. This prevents a misleading unknown shell effect and makes the intended route explicit; it does **not** by itself prove a committed learned note or reduced rereads.

Freeze a fresh simultaneous PTC control versus opt-in memory pair on six previously absent confirm tasks: Go `arcane-drift-detection-baselines` and `dasel-html-document-format`, TypeScript `cliffy-config-file-parsing`, JavaScript `katex-multicolumn-array-spans`, Rust `pest-character-class-coalescing`, Python `returns-validated-error-accumulation`. A bounded search of existing `.artifacts` found no previous trial path for these six; both dry plans select the same tasks/order from confirm manifest SHA `f30a3bb6245a360820be5684ed98d001cea460169168633c18487ef28d48fad4`. Luna/max via authorized OpenRouter, one attempt/zero Harbor retries, 32,768 output reserve, 1B task-input ceiling, three concurrent trials per arm. PTC and memory share the E13 source changes; only the active memory profile asks the model to use the virtual CLI. Results roots will be `.artifacts/live-e13-ptc-control-20260915` and `.artifacts/live-e13-memory-route-fix-20260915`; record both source diff hashes and exact terminal paths before conclusions. No selective retry or case replacement.

Prespecified E13 gates: compare paired Harbor rewards and local completed-evidence status first; then same-SHA repeat/read-request behavior on a majority of tasks, physical acquisition, provider-visible source egress where mappable, calls, cached/uncached input, cost and active wall, actual note writes/recovery calls, compound-route blocks, worker epochs/cuts, and unknown effects. Memory requires no quality regression, no false verified completion, fewer repeated reads on most pairs, and aggregate calls/cost no worse to count as a net improvement. If the virtual route is not exercised, do not credit it; if it is exercised but repeated reads persist, inspect model-selected read windows and retained-value use separately from note availability before adding another prompt packet or broker abstraction. E12's Anko regression remains a quality counterexample; a new six-task pass alone cannot prove its root cause is fixed.

E13 launch (13:48 UTC): both result roots were fresh and both metadata files record the same source diff SHA `6c50c287795bbb557727999c91bea5932bc50badf6b0b42ca613b6ef24fbd883`, manifest SHA `f30a3bb6245a360820be5684ed98d001cea460169168633c18487ef28d48fad4`, Luna/max, one attempt, three concurrent each and zero Harbor retries. Both six-task jobs were accepted and are in flight. The local/focused contract checks passed; do not infer Harbor reward from an in-flight `result.json`. Freeze source/config and do not start a duplicate or replacement trial. Current source state is not a clean Git commit; the recorded SHA, each run's metadata, profile bytes and terminal trace paths are the comparison identity.

E13 terminal audit (14:25 UTC): all twelve Harbor jobs ended, one attempt each, zero Harbor retries/errors. Raw Harbor rewards were PTC **2/6** (Arcane and Returns) versus memory **0/6**. However KaTeX, Pest and Returns stopped inside the agent in **both** arms with nonretryable OpenRouter 402 credit exhaustion at the frozen 32,768-token output reserve. Their Harbor rewards grade whatever partial patch was submitted; Returns control scored 1 after the provider stopped it, so these three pairs are a separate provider-interrupted stratum, not clean completed-agent comparisons or evidence that lowering output reserve is safe. The first three pairs reached normal local `blocked/verified:false` agent outcomes and independent Harbor verification: PTC Arcane/Cliffy/Dasel 1/0/0, memory 0/0/0. The quality no-regression gate fails on Arcane. All six first-three local completions refused a verified claim; no false completed-evidence answer was observed. The other six local results are `failed/verified:false` with `runtime_failed` OpenRouter 402, not Harbor/eval failures. Do not selectively retry, replace, or change budgets within E13.

On the first three verifier-complete/provider-uninterrupted pairs, model calls were PTC 65/90/49 versus memory 48/88/48 (204→184 total); uncached input 471,814→473,902; cached read 17,865,986→17,140,470; output 150,883→161,981; provider-request bytes 92,589,604→86,853,676; API-equivalent cost $0.656302→$0.655635 (effectively tied); summed active trial wall 2,699→2,612s (not concurrently elapsed batch wall). Different quality/stopping trajectories make efficiency descriptive, not causal. Completed `fs.read` requests by task Arcane/Cliffy/Dasel were PTC 37/44/29 versus memory 40/31/27; physical source lines 4,154/5,855/4,232 versus 3,007/4,588/2,866; catalog-served same-SHA repeat lines **894/876/0 versus 1,930/1,524/316**. Totals: requests 110→98, physical lines 14,241→10,461, repeat lines **1,770→3,770**. Physical-fetch savings and model reread demand diverged again; these repeat lines are broker-served `fs.read` content, not a measured count of distinct provider-visible source lines. All three memory traces committed three note versions; the new compound-command rejection branch was not exercised, so do not credit it for the live read/cost results.

Arcane is a real quality regression in the submitted patch, not a Harbor setup error: memory's independent `go build -tags exclude_frontend ./...` wiring gate failed because its authored `drift_detection_service.go` referenced nonexistent `ContainerInspectResult.Config` and `.HostConfig` fields at lines 475–485. Its new/base CTRF reports stayed empty because the packages did not build; the verifier correctly scored 0/82 requested and 0/2 preserved tests. PTC's patch scored reward 1. Both agent trajectories later timed out a PTC cell with an unresolved shell effect and locally blocked (PTC canonical seq 394/403; memory 468/480); the stricter local completion gate withheld both, while Harbor separately graded the available patch. Memory's Arcane worker remained live before the continuation, wrote notes, and explicitly reused retained handles `read:9` at seq 220 and `read:3` at 286. Nonetheless 1,848 of its 1,930 same-SHA repeat lines followed its first seq-183 navigation; settings_service.go and huma.go each accounted for 625 and 572 repeated lines. Its navigation included a note receipt/heading and newest read refs but not the full learned finding text; the model made no `working_set` query. This suggests a recovery-salience gap, but is not proof that navigation caused the compile defect or that all overlapping focused windows were avoidable.

Terminal result and trace location contract: for each row below, take the arm root recorded above, then `<NNN>-<task>-attempt-01/<NNN>-<task>-attempt-01/<trial-id>/result.json`; the canonical trace is in that same trial at `agent/skein-state/runs/<run-id>/events/<events-file>`. Both roots' `runs.jsonl` additionally contain `result_paths`, Harbor rewards, errors and trial metrics; each trial's `agent/skein-state/evaluation/result.json` records its actual local status/error, usage and event directory. The run/event IDs are common across arms for a task, while trial IDs differ.

| Task (NNN) | PTC trial ID | Memory trial ID | Run ID / canonical events file |
| --- | --- | --- | --- |
| Arcane (001) | `6b6c66c21be08226b4f6b91f43dc2c35__SinqtQj` | `6b6c66c21be08226b4f6b91f43dc2c35__XzUF7Lg` | `e33e563585a1637a9db3756923e8cf13` / `afd2bd8f5ab7d7f3be35a8b12e20c0aa0a558dd372b75e44dca155f33f7193ab.jsonl` |
| Cliffy (002) | `3db686ba7c0e904ddb4750b14eba8405__GezHeez` | `3db686ba7c0e904ddb4750b14eba8405__EZxrVMK` | `06191237a66974adbfbe7320b23f6f6e` / `6efad9381dbbc211ef1065e7ad63de355c5fcd3453dd8188680a24ff4100e078.jsonl` |
| Dasel (003) | `ed40e92c29778c2488fa271593417cf4__kZ8XYhA` | `ed40e92c29778c2488fa271593417cf4__LVcqKAD` | `1e63a52f5755e85042150d53e5ffdfe2` / `5e6f29334124724026483ace60a224eefd8b7c917353fc83b72f5c08b77a9453.jsonl` |
| KaTeX (004) | `27da72e69f984905e100e9d374dfe5b5__4hc9fXS` | `27da72e69f984905e100e9d374dfe5b5__huS3BWr` | `0662a1fd62690051aa720e12aec6b5a6` / `e84f9b806d19ac972d77020ec98e6b0c2692b08075cd454a6f63b03333c7a2f2.jsonl` |
| Pest (005) | `2db976b6eeb361bd715b74de2d1ad66d__ceo83Ao` | `2db976b6eeb361bd715b74de2d1ad66d__VWnaNGB` | `d71421bdbfd2fbe4579c19b9f7c85b4a` / `0c28ac76df3fef6e5ff636bfcff96ede72d3acb38927503092af757a7ba5a67b.jsonl` |
| Returns (006) | `c3b8a00551c152d9c2cca35ae1a1ff84__sFdWPsf` | `c3b8a00551c152d9c2cca35ae1a1ff84__WXfGZUc` | `022cefb97fc3f5add139e9b184a16674` / `0dfd9c3cc0487071ffd534473f671e4aac0d90d43af20ec61b835f48530d66d7.jsonl` |

Decision: E13 does not qualify memory defaults or reliable memory use. The routing correction remains deterministic safety/usability hardening, not an empirical call/cost/reread gain. No further paid trial may start under an exhausted OpenRouter balance; a fresh, explicitly frozen provider-backed experiment requires restored credit or a separately justified changed provider/budget contract. Offline next direction should measure exact same-SHA interval overlap versus justified narrower inspections and selected PTC output/provider-wire exposure; then test a distinct demand-side mechanism that makes an evidence-linked finding or retained read result directly actionable in the next cell without suppressing needed source content. Preserve source-freshness probes, shell uncertainty and independent verification. Do not repeat another small navigation-budget or note-prompt tweak without a trace-backed target and fresh live gate.

Further canonical interval audit (14:30 UTC, six first-three event files): union prior ranges by `(path, sha256)` at each completed `fs.read`; count a request as fully covered only when every returned line was already in that version's prior captured ranges. PTC versus memory fully covered requests were Arcane 9→17 (fully covered lines 723→1,924), Cliffy 3→2 (348→700), Dasel 3→4 (445→216). All-earlier same-SHA overlap lines were Arcane 894→1,930, Cliffy 1,220→1,524, Dasel 445→316. This is more precise than the broker's `read_reuse.reused_lines`: the broker can serve a historical-overlap range from source instead of the live catalog after an epoch change or other eligibility loss, so physical/catalog attribution and semantic overlap must not be equated. Arcane is the dominant clean fully-covered demand regression; Dasel overlap actually fell and Cliffy's increase is partly covered narrower windows. The existing worker already rewrites exact retained-text repeats in selected output to a short `[source already retained as read:N]` notice. Arcane has two such selected-output artifacts for `read:9` and `read:3`, and its later cells explicitly called `agent.state.reuse` for both; suppressing more source content or adding generic eager binding notices could harm coding quality and cost. A candidate next gate, not yet implemented or paid, is a bounded, non-suppressive, read-time indication of the *specific prior live handle and covered interval* on a wholly-covered request, visible in the completed PTC cell even when its code prints only `data.text`. It must keep exact source bytes available, use the existing worker/catalog authority, be triggered only on same-SHA full coverage, avoid per-call global packet growth, and show fewer subsequent fully-covered requests without worse Harbor quality/cost in fresh paired live trials. First verify actual selected output and wire exposure; a provider request byte count is not a duplicated-source count. Credit restoration is required before such a canary can test value.

Selected-response exposure audit (15:05 UTC) changes the candidate priority. Inputs were the same six first-three canonical events, all 40/37 Arcane, 31/44 Cliffy and 27/29 Dasel captured read-result artifacts per memory/PTC arm, and all corresponding ADK `execute_code` function-response `model_text` records from each trial's `agent/skein-state/adk/sessions.db`; no read artifact or completed-cell response was missing. These ADK records are inspected as model-facing delivery evidence, **not** as a second historical authority. Before applying the existing `evals.memory_audit.audit_emissions` exact nontrivial unique-line mapper, the common PTC printed-source renderer `N: line` was mechanically stripped from selected response lines; the mapper already handles direct-read `N | line`. The mapper leaves short/ambiguous/transformed lines unmapped and does not include shell/artifact routes or complete HTTP provider-request bodies. One Arcane fully-covered 400-line catalog hit at canonical seq 200 produced a 31 KB full selected-output artifact, yet its bounded ADK function response contained 15.4 KB and matched only 39 of that read's 262 eligible nontrivial lines; broker-served repeat lines materially overstate this sample's selected exposure.

Correction (15:12 UTC): the first one-off preprocessing used a double-escaped `N: line` regex and did not strip the PTC renderer as stated above. The corrected `evals.memory_audit.audit_emissions` program now recognizes `N: line` for PTC output directly, with a deterministic regression test. Its measurement SHA is `021fdc8fed585f59703829282660b1899bea3178f7690e78d0c11ebc5930da8b`; the table below is the corrected program's result on the exact E13 selected responses. The previous 608→298/51% and Arcane 154→222/44% claims in the surrounding prose are superseded by 613→331/46% and 159→226/42%, respectively; the qualitative direction is unchanged.

Provider-credit check (15:12 UTC): one read-only call to OpenRouter's [current-key status endpoint](https://openrouter.ai/docs/api/api-reference/api-keys/get-current-key) returned HTTP 200 but both `limit` and `limit_remaining` were null; it cannot establish whether the account balance was replenished. The last trial evidence remains nonretryable 402 credit exhaustion. Do not launch paid work on an unverified assumption of restored capacity; this check did not consume a model request or change E13 outcomes.

| Clean paired task | PTC selected mapped source lines | Memory selected mapped source lines | PTC duplicate mapped lines across selected responses | Memory duplicate mapped lines |
| --- | ---: | ---: | ---: | ---: |
| Arcane | 1,429 | 1,084 | 159 | 226 |
| Cliffy | 2,480 | 1,537 | 385 | 105 |
| Dasel | 947 | 38 | 69 | 0 |
| **Total** | **4,856** | **2,659** | **613** | **331** |

Thus memory **reduced conservatively mapped repeated source lines in bounded model-facing PTC responses by 46% overall and on two of three clean tasks**, while Arcane increased 42%. This is a partial selected-egress improvement, not evidence of fewer read requests, fewer broker catalog repeats, a complete provider-wire duplicate count, or a quality win. It also does not prove the model semantically used a learned finding. The prior blanket candidate to add notices on every full catalog hit is **not yet justified**: the existing sanitizer and bounded response already reduce selected source egress on Cliffy/Dasel, and expanding it could conceal needed code at a quality cost. Next offline analysis should isolate Arcane's 17 fully covered *requests* by cell and test whether an exact actionable retained handle was visible before each; the paid gate, once credits return, must measure first-order quality as well as selected/source/provider-wire duplication rather than treating broker reuse alone as a failure or a gain.

Arcane cell-level follow-up (16:35 UTC, read-only): the 17 fully covered same-SHA requests are concentrated in seven submitted PTC cells, at canonical completed-read sequences 129; 200/202; 209/211/213/215; 258/262/264; 403/405/407/409/411; 418; and 448. The first group precedes the seq-182 navigation; nine requests follow it before seq 308; seven follow seq 308. Both nav-182 and nav-308 reported a live worker in the same kernel epoch, but their bounded navigation content contained no `read:N` access handles; nav-182 carried only the two newest read artifact references and marked six older reads omitted. No submitted Arcane cell called the already-implemented `agent.state.reads(path=...)` targeted catalog listing, although seq 220 and 286 explicitly used `agent.state.reuse` handles. These are observations about availability/exposure and model selection, not proof that all 17 rereads were unnecessary. The seven-cell clustering changes the candidate: a notice emitted *after* an `fs.read` batch cannot prevent other covered requests already chosen in that same batch. Before adding another read-time notice, test a bounded **pre-read, path-targeted** use of the existing `agent.state.reads(path=...)` recipe against fresh paired live tasks, preserving explicit current-SHA freshness checks and exact source access. Freeze quality, fully covered request count, selected/provider-visible egress, calls, uncached input, cost, and terminal reasons as gates; do not claim value or dispatch while provider capacity is unverified.

Reassessment (17:33 UTC): **retract both the pre-read enforcement candidate above and the exact-live-object redirection candidate** as proposed next implementations. The PTC broker already tests current source SHA and reuses exact/partial captured ranges (`app/agent/ptc.py::_catalog_read`); worker registration already preserves the same `read:N` handle for an exact same-range reuse and suppresses exact repeated selected text (`harness/ptc/repl/worker.py::register_read`), with the exact/partial/changed-source lifecycle checked in `tests/unit/test_notebook_ptc_integration.py::test_ptc_read_catalog_reuses_exact_and_partial_ranges_but_not_changed_source`. A mandatory `agent.state.reads(path=...)` call would repeat work without ensuring fewer model-requested reads. Reusing the original mutable Python result object instead of the broker's attested refreshed response would not address call count or reread demand and could weaken provenance. Also, `identity_probe_lines:1` is a *selected-line* accounting field: `harness/execution/tools/coding.py::execute_read` loads whole-file bytes to compute the SHA even for that probe, so `source_read_lines:0` is not zero filesystem I/O. Arcane's 17 fully covered requests are an upper-bound opportunity signal, not a classification of 17 unnecessary decisions. E13 seq-200 already reused all 400 selected lines (`source_read_lines:0`, one identity probe) yet remained a model-requested capability. No new PTC/memory mutation or paid ablation is justified by this reread evidence. Preserve the existing catalog, freshness and completion gates; treat model planning/verification loops and independent quality/cost as separate unresolved questions.

Read-only call-loop/quality audit (18:58 UTC): direct ADK `execute_code` function responses and terminal Harbor reports were checked task by task for the earlier eight-task Muse held-out pair and the E12/E13 Luna pairs; canonical events remain the authority for cell/effect outcomes, while ADK responses show what rejection the model actually received. The older Muse cohort's 554→401 aggregate call reduction is dominated by two early-stopped failures: Drizzle 90→15 calls (memory changed no files, F2P 0/130) and Narwhals 82→24 (memory F2P 0/103 but locally `complete/verified:true` under that older completion contract), a combined 133 of 153 fewer calls. In E12, Pebble scored Harbor 1/1 in both arms and had 102→77 model calls, but control received 30 consecutive `execute_code` blocks after one timed-out unknown-effect cell versus seven in memory: 23 of the 25-call pair difference. Pebble memory committed zero notes, so this is not a learned-memory call win. In E13 Arcane, the 65→48 call difference equals the pair's 24→7 immediately blocked `execute_code` responses after a timed-out unknown effect; memory's independent Go build failed on nonexistent `ContainerInspectResult.Config`/`.HostConfig` fields while control's patch scored Harbor 1. Across the E13 clean three, net blocked-response difference was 14 of the 20 fewer memory calls (Arcane -17 offset by Dasel +3). These blocks correctly preserve effect uncertainty, but repeated model invocations against an unchanged refusal add no execution progress. They are a **workflow terminalization/reconciliation** target shared by both arms, not a reason to weaken safety or to credit memory. Receipt/fingerprint-backed reconciliation may resume only proven outcomes; otherwise a structured blocked terminal should end further paid turns. This is a trace-backed proposal, not implemented or live-validated.

Quality misses are task-specific: E12 Awilix both arms missed `scope.initialize()` without parent initialization; E12 Anko memory returned generic `syntax error` instead of `invalid default argument declaration` (control passed), after seven distinct cells had rejected note citations plus schema/command errors. E13 Cliffy both arms missed nested config objects; Dasel both missed implicit nested-list closing; Arcane memory failed compile as above. The older eight-task paired quality tie 4/8 hid two memory-only wins (Arktype deep refs, HTTPX streaming) and two control-only wins (Narwhals rolling, Prometheus typed-label ordering), with severe early memory stops on Drizzle/Narwhals. E12 HTTPX control's seventeen `review`-phase cells were all distinct substantive edits/checks, not an unchanged-workspace review loop. Most recent task calls are model-generated PTC cells; their phase labels alone do not classify wasted reasoning. No recent DeepSWE task compacted; prior-run recall was not exercised in the older held-out cohort. The intact-context automatic memory/navigation path therefore has no demonstrated net quality/cost value here. Preserve enabled-but-opt-in continuity capability and do not run another read/catalog or navigation-size variant merely to improve a surrogate metric.
